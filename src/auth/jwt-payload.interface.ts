// src/auth/jwt-payload.interface.ts

export interface JwtPayload {
    username: string;  // The payload will contain the username or any relevant information
    // Add other fields if needed
  }
  